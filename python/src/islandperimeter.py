class Solution:
    def islandPerimeter(self, grid: List[List[int]]) -> int:
        return 0
    #     // go through all of cells
    #     //    and return the cells that is connect
    #     //    and return thet neighbors for each cell
    #     [totalLandCells, totalNeighborsForLandCells] = filterConnectedLand()
    #     return totalLandCells*4 - totalNeighborsForLandCells
    #
    # def filterConnectedLand():